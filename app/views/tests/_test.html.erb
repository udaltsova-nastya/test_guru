<tr>
  <td><%= test.category_title %></td>
  <td><%= link_to test.title, test_path(test) %></td>
  <td><%= test.level %></td>
  <td><%= test.questions_count %></td>
  <td><%= link_to "Edit", edit_test_path(test) %></td>
  <td><%= link_to "Delete", test_path(test), method: :delete, data: { confirm: "Are you sure?" } %>    
  </td>
</tr>